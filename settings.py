import pygame

#set ups, made them caps so easier to identify when importing
SCALE_FACTOR = 3
FPS = 60
ZOOM = 1.3
GRAVITY = 1.3
SPEED = 6 # was 10 moved to 8 to make a bit slower
SCREEN = pygame.display.set_mode((1, 1))

